/**
 * @fileoverview
 * Provides methods for the mapping feature of the hitlist project.
 */

/** global namespace for relationshipanalytics projects. */
var ra = ra || {};

/** hitlist namespace for hitlist project. */
ra.hitlist = ra.hitlist || {};

/** hitlist namespace for hitlist editing. */
ra.hitlist.mapping = ra.hitlist.mapping || {};

ra.hitlist.mapping.loadTest = function(){
	console.log("Helloooo");
};
	
